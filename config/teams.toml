# =============================================================================
# SDWA — Team catalog configuration
#
# IMPORTANTE: Este arquivo contem membros PLACEHOLDER.
# Substitua pelos nomes, skills e webhooks reais da equipe antes de
# habilitar allocation.enabled = true.
#
# Skills devem corresponder as frentes do MegaHub (case-insensitive):
#   ams fi, ams hr, ams mm, ams sd, ams abap, ams grc ac, ams pm, ams ps,
#   ams co, ams qm, ams ewm, ams ccs fi/ca, ams ccs dm, ams ccs billing,
#   ams sap business one, ams solman, ams basis, basis,
#   csg hr, csg mm, csg abap, csg sd, csg fi, csg ps, csg pm,
#   multiplataforma, integration & df-e, portal, infra
# =============================================================================

# ---------------------------------------------------------------------------
# Members — substitua pelos nomes reais
# ---------------------------------------------------------------------------

[[members]]
id = "dev-fi-01"
name = "Dev FI 01 (substitua)"
role = "developer"
skills = ["ams fi"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "dev-hr-01"
name = "Dev HR 01 (substitua)"
role = "developer"
skills = ["ams hr", "csg hr"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "dev-mm-01"
name = "Dev MM 01 (substitua)"
role = "developer"
skills = ["ams mm", "csg mm"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "dev-abap-01"
name = "Dev ABAP 01 (substitua)"
role = "developer"
skills = ["ams abap", "csg abap", "abap"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "dev-sd-01"
name = "Dev SD 01 (substitua)"
role = "developer"
skills = ["ams sd", "csg sd"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "dev-basis-01"
name = "Dev BASIS 01 (substitua)"
role = "developer"
skills = ["basis", "ams basis"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "dev-grc-01"
name = "Dev GRC 01 (substitua)"
role = "developer"
skills = ["ams grc ac"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "dev-pm-01"
name = "Dev PM/PS 01 (substitua)"
role = "developer"
skills = ["ams pm", "ams ps", "csg ps"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "dev-ccs-01"
name = "Dev CCS 01 (substitua)"
role = "developer"
skills = ["ams ccs fi/ca", "ams ccs dm", "ams ccs billing"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "dev-multi-01"
name = "Dev Multi 01 (substitua)"
role = "developer"
skills = ["multiplataforma", "integration & df-e", "portal"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "dev-b1-01"
name = "Dev B1 01 (substitua)"
role = "developer"
skills = ["ams sap business one"]
active = true
webhook_url = ""
max_concurrent_tickets = 5

[[members]]
id = "coordenador"
name = "Coordenador (substitua)"
role = "coordinator"
skills = []
active = true
webhook_url = ""
max_concurrent_tickets = 0
# Frentes gerenciadas por este coordenador.
# Se vazio ou ausente, recebe sugestoes de TODAS as frentes.
# Preencha para filtrar, ex: ["ams fi", "ams hr", "csg hr"]
managed_fronts = []

# ---------------------------------------------------------------------------
# Allocation rules
# ---------------------------------------------------------------------------
[allocation]
enabled = false

strategy_order = ["skill_match", "current_load", "historical_load", "alphabetical"]
max_suggestions = 3

# Status NOVO — quais status indicam chamado novo na fila
novo_status_labels = ["Novo"]

# Status de conclusao (fechamento do chamado)
completion_status_labels = ["Fechado", "Resolvido", "Cancelado"]

# Status que indicam retorno ao desenvolvedor
# CORRIGIDO: nao usar "Em Processamento" generico — usar apenas status
# que indicam rejeicao/revisao explicita pelo cliente
return_to_developer_labels = ["Não Homologado", "Revisão"]

# Timeout de aprovacao (minutos) antes de registrar alerta
approval_timeout_minutes = 60

# Limite maximo de chamados novos por ciclo para enviar sugestoes.
# Na primeira execucao, a fila pode ter 100+ chamados "Novo".
# Este limite evita bombardear o coordenador.
# Chamados alem do limite sao logados mas nao geram sugestao.
max_new_tickets_per_cycle = 10
